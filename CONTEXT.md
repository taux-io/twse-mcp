# 台股公開資料 MCP

把臺灣證券交易所的公開資料，包裝成任何 AI 都能查詢的 MCP 服務。這份文件只定義詞彙——
決策紀錄在 `docs/spec-workers-migration.md`，使用說明在 `README.md`。

> `_Avoid_` 只約束**散文中指稱同一概念的競爭說法**。它不管普通詞彙的一般用法
> （「代號清單」不是在講目錄），也不追溯既定的識別字與欄位名（`include_realtime`
> 是 API 契約的一部分，不因這份文件改名）。

## 市場與機構

**上市**：
在臺灣證券交易所掛牌交易的股票或 ETF。本服務的資料涵蓋以此為主。
_Avoid_：TWSE 上市、集中市場、listed

**上櫃**：
在證券櫃檯買賣中心掛牌交易的股票或 ETF。本服務**只能取得其盤中即時報價**，
取不到歷史與統計資料（來源封鎖雲端連線，見 spec）。
_Avoid_：OTC、店頭市場（指市場別時）

**證交所**：
臺灣證券交易所。本服務絕大多數資料的來源，也是即時報價站的營運者。
_Avoid_：TWSE（中文行文中）

**櫃買中心**：
證券櫃檯買賣中心，上櫃市場的營運機構。是**機構**，與「上櫃」這個市場別不同——
提到組織時用本詞，提到掛牌狀態時用「上櫃」。
_Avoid_：TPEx（中文行文中）

## 資料

**資料集（dataset）**：
證交所 OpenAPI 的一個 endpoint，對應一張完整的公開報表。每個資料集**不吃任何查詢參數**，
一次回傳整份資料。以 `dataset_id`（例如 `exchangeReport/STOCK_DAY_ALL`）識別。
_Avoid_：表、API、endpoint、table
（例外：`README.md` 面向不寫程式的讀者，刻意改稱「報表」——那是對外的白話翻譯，不是另一個概念。）

**目錄（catalog）**：
所有資料集的清單，含說明、分類與欄位定義。由證交所的 swagger 在**建置期**產生並簽入版控，
執行期不再抓取。
_Avoid_：資料集清單、index（指這份目錄時）

**代號欄位**：
資料集中用來識別個股／基金的欄位。各資料集命名不一致（`Code`、`基金代號`、
`ETFsSecurityCode` 等），因此需要自動偵測。
_Avoid_：股票代號欄、ticker 欄、code column

**即時報價**：
來自證交所基本市況報導站的盤中價格，約每 5 秒更新。它與資料集是**不同來源**：
資料集最新只到前一交易日，且即時報價同時涵蓋上市與上櫃。
_Avoid_：即時價、盤中價（單獨使用時；「盤中即時報價」可）

**caveats**：
隨查詢結果一併回傳的提醒，說明某段資料為何缺漏、或某個數字為何不可盡信
（例如市值粗估不等於基金規模）。用意是讓 AI 知道**哪些數字不能當真**。
中文行文中沿用原字（工具描述即寫「記在 caveats」），因為它同時是回應中的欄位名。
_Avoid_：但書、警告、注意事項（指這種提醒時）
（例外：`README.md` 對一般讀者說「提醒」。）

## 協定

**協定 era**：
MCP wire 詞彙與分派規則的一整代。目前有兩代：**legacy**（`2025-11-25` 及更早）與
**modern**（`2026-07-28` 起）。兩者不是同一套訊息的新舊版本，而是兩套並行的詞彙——
modern 取消了 `initialize` 交握與 session，改由每個請求自帶協定資訊。
_Avoid_：協定版本、新版／舊版、protocol version（指這個概念時）

**協定修訂版**：
`2026-07-28` 這種日期字串，識別某個 era 的一次具體修訂。它**不是可協商的版本號**：
modern era 沒有交握，client 每個請求自報修訂版，server 逐一接受或拒絕。
_Avoid_：協定版本、版號、version

**lane**：
同一個端點上分派給某一個 era 的處理路徑。本服務的 `/mcp` 同時開著 legacy 與
modern 兩條 lane，規範稱這種 server 為 dual-era。
_Avoid_：通道、路徑、分支、相容層

**era 收斂**：
關掉其中一條 lane、只服務單一 era 的動作。與「遷移」不同——遷移暗示換掉一個值，
收斂則是停止服務一群既有 client。
_Avoid_：遷移、升級、切換（指這個動作時）

## 命名慣例

**`twse_` 前綴**：
所有對外工具的前綴，標示資料來自證交所——包含即時報價站（它由證交所營運，
且同時涵蓋上市與上櫃）。前綴標示的是**來源**，不是市場別。
